services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile-prod
    restart: unless-stopped
  
  proxy:
    build: ./proxy
    ports:
      - 80:80
    depends_on:
      - backend
    restart: unless-stopped